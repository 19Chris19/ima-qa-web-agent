# 更新日志

本文件记录公开 Provider A 发布包的用户可见变化。账号登录态、共享知识库原文、会话历史和运行日志不会进入 GitHub。

## [0.3.0] - 2026-09-15

### 网页问答与账号恢复

- 兼容 IMA 流式状态通知和结构化来源；缺失、失败或重复终态不再按成功处理。
- 账号检查有界且不隐式刷新；显式刷新后复检，管理页显示结果和时间，区分暂态错误与登录失效。
- 重新登录绑定原账号身份，错误身份不覆盖原凭据；按钮防重复点击，失败后重读服务端状态。
- 已派发的问答不再因会话或网络错误自动重发，避免重复调用。
- 清理回答末尾内部引用标记，并修正浏览器接入模式说明。
- 修复管理页样式覆盖隐藏属性的问题，未启用区块和已隐藏登录表单不再意外显示。

### 原生知识库 Agent 与接入

- 新账号库默认使用 `knowledge_agent`；已有账号库继续使用已记录的模式，管理员可显式升级新会话，历史会话保持原模式。
- 扫码接入后自动运行一次隔离的知识库问答验证。验证失败会保留账号但不授予调度资格，并提供手动重试入口。
- 只有单次派发、单个成功终态、非空回答及目标知识库来源同时成立，账号才可用于默认原生模式。
- 账号存储使用 generation 比较和跨进程锁，避免旧进程快照覆盖新的账号资格；容量摘要确认运行池已应用对应 generation。

### 二次开发与验证

- 增加终端机器人适配示例、用户会话隔离及内部持久幂等接口说明；示例不连接微信或 Android。
- 增加独立部署的真实 IMA 验收指南，覆盖扫码、来源、追问、重启恢复和并发演练。
- Provider A 自动化测试 `233/233` 通过；30 分钟合成演练完成 9,127 次问答和 536 次幂等重放，失败与资源泄漏均为 0，最终队列清空。
- npm 白名单包共 109 个文件；不包含真实账号登录态、密钥、运行目录、知识库原文或会话记录。
- 维护者没有隔离的授权 IMA 账号，因此本次未验证真实扫码、共享库来源质量或上游容量；部署者应按验收指南使用自己的授权账号完成真实验收。

升级前私密备份 runtime 及加密密钥，停止旧实例后替换程序，再使用同一私有存储启动。不要同时运行两个写入同一账号库的版本。回退时停止新实例，恢复上一程序版本及配套备份。七天会话保留方式不变；既有部署须显式升级模式，新会话才使用 `knowledge_agent`，历史会话仍保持原模式。

## [0.2.1] - 2026-08-21

### 账号接入体验

- 管理页接入账号默认打开独立、可见的 IMA 登录窗口；每次接入使用新的临时浏览器资料，避免复用本机微信快捷登录或已有账号身份。
- 登录成功后，服务会验证共享知识库、加密保存登录态、同步账号池，并自动关闭临时浏览器和管理页接入窗口。
- 接入任务支持随时取消并立即关闭管理页窗口；未完成、超时或验证失败的登录态不会进入账号池。
- 后台二维码模式保留为显式兼容排查选项，不再作为推荐的默认接入路径。

### 并发演练体验

- “开始真实演练”改为页面内二次确认，不再依赖部分内嵌浏览器会阻塞的系统确认框。
- 管理页 HTML、脚本和样式统一禁止缓存，避免更新后出现新页面加载旧脚本、按钮无响应的版本错配。
- 本地三账号基线验收完成：3 条首问和 3 条追问均成功，峰值真实 IMA 并发为 3；账号覆盖、会话隔离和追问连续性通过。

## [0.2.0] - 2026-08-20

### 账号池演练

- 管理页新增受保护的“并发演练”：可用真实 IMA 共享知识库运行基线并发、两倍账号数排队压力和 `1-30` 位自定义模拟客户。
- 演练中的首问和追问复用现有账号池、队列和 IMA 会话粘滞；服务自动验证账号覆盖、会话隔离和追问连续性。
- 演练期间普通问答会返回明确维护状态，结束、取消或重启后自动恢复；演练不会刷新、覆盖或导出账号凭证。
- 报告按模拟客户展示问答、精选来源、检索摘要、时延、失败分类和人工质量评分，并以脱敏 JSON 保存在私有 `runtime/` 中，默认保留 7 天。
- 内置评测题库当前有 50 道 3DGS 问题，单次最多 30 位模拟客户；自动分配不会循环复用题目，题库不足时会要求维护者填写首问。
- 模拟客户、首问、追问和报告复核都支持单独展开/收起，并提供全局展开、全局收起和关闭当前复核视图。

### 账号接入

- 管理页新增受保护的二维码接入流程：输入账号名称后，在当前页面扫码登录 IMA。
- 登录成功后服务端自动验证共享知识库、加密保存登录态并同步账号池；二维码和临时浏览器资料会清理。
- 每次接入使用全新临时浏览器资料，服务会拦截快捷登录；未确认二维码模式的登录态不会写入账号池。
- 接入成功后使用服务端不可逆 IMA 身份指纹阻止同一账号换名称重复入池。
- 旧账号库发现重复身份时自动停用重复条目，避免虚增账号池并发；管理页会显示原因，确认后可删除。
- 管理页二维码接入默认使用后台临时浏览器；只有二维码无法回传时才提供可见浏览器兜底。
- 保留 CLI 接入作为纯 Linux 或 Docker 部署的兼容兜底。

## [0.1.1] - 2026-08-19

### 问答展示

- 改善流式回答中的标题、段落、表格、代码块、引用、有序列表和无序列表展示。
- 兼容 IMA 常见的 `<br>` 换行、紧凑标题、紧凑列表和非标准代码围栏输出。
- 表格使用语义化 HTML 表格，移动端保留列标题，避免用户看到原始 `|` 和分隔线。
- 未闭合的流式 Markdown 标记不再直接泄漏到页面。
- IMA 把标题和整段正文黏在一行时，自动降级为普通段落，避免错误的视觉层级。

### 会话与发布

- 保持现有多会话隔离、7 天历史保留、精选来源和双账号池行为不变。
- Provider A 生产冒烟验证通过：SSE 正常结束，真实来源和结构化回答可以在页面中展示。
- 发布包仍不包含 `.env`、`runtime/`、浏览器 profile、cookie、refresh token、账号库密钥或真实问答历史。
- npm 发布物改为显式白名单，未跟踪的本地文件不会被意外打入包中。

### 验证

- Provider A 全量 Node 测试：156 项通过。
- 真实 3117 生产入口验证：健康检查通过，2 个账号可用；真实格式问答包含表格、有序列表和引用，未发现 Markdown 控制符泄漏。
